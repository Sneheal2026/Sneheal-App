// App-level constants
// Add API URLs, config values, feature flags, etc.

export const APP_CONFIG = {
  APP_NAME: 'Sneheal',
  COUNTRY_CODE: '+91',
  OTP_LENGTH: 6,
  OTP_RESEND_SECONDS: 30,
} as const;
