import React from 'react';
import { View } from 'react-native';

/** Placeholder tab screen — navigation is handled by the custom scan tab button. */
const ScanPlaceholderScreen = () => <View />;

export default ScanPlaceholderScreen;
